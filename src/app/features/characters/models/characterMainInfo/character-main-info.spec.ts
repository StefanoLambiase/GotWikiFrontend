import { CharacterMainInfo } from './character-main-info';

describe('CharacterMainInfo', () => {
  it('should create an instance', () => {
    expect(new CharacterMainInfo()).toBeTruthy();
  });
});
