import { CharacterRelationships } from './character-relationships';

describe('CharacterRelationships', () => {
  it('should create an instance', () => {
    expect(new CharacterRelationships()).toBeTruthy();
  });
});
