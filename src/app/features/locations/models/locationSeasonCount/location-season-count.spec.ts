import { LocationSeasonCount } from './location-season-count';

describe('LocationSeasonCount', () => {
  it('should create an instance', () => {
    expect(new LocationSeasonCount()).toBeTruthy();
  });
});
